// TODO: dynamically adjust by viewport size / allow for configuration
export const CANVAS_WIDTH = 800;
export const CANVAS_HEIGHT = 600;

export const TILE_A_COLOR: [number, number, number, number] = [
  0.0, 0.9, 1.0, 1.0,
]; // teal
export const TILE_B_COLOR: [number, number, number, number] = [
  1.0, 0.4, 0.3, 1.0,
]; // coral
